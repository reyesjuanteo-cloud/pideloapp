import { AdminMensajeros } from "@/features/admin/mensajeros";

export const metadata = { title: "Aprobación de mensajeros — Pídelo" };

export default function AdminMensajerosPage() {
  return <AdminMensajeros />;
}
