import { PanelProveedor } from "@/features/servicios/panel-proveedor";

export const metadata = { title: "Tus servicios — Pídelo" };

export default function PanelProveedorPage() {
  return <PanelProveedor />;
}
