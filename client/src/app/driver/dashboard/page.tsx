import { DriverDashboard } from "@/features/driver/driver-dashboard";

export const metadata = { title: "Panel de domiciliario — Pídelo" };

export default function DriverDashboardPage() {
  return <DriverDashboard />;
}
